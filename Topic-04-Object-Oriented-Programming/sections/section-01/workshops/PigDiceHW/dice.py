import random


class Dice:
    """A single six-sided die."""

    @staticmethod
    def roll() -> int:
        """Roll the die and return a random value 1–6.

        Returns:
            int: A random integer between 1 and 6 (inclusive).

        Example:
            Dice.roll() -> 4
            Dice.roll() -> 1
        """
        pass
